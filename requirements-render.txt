# Superset of requirements.txt for a persistent host (Railway) that also needs
# to run submit_houdini_render. Deliberately kept separate from
# requirements.txt: Vercel's lean serverless deployment installs from
# requirements.txt only and must NOT gain ciocore (see conductor_render.py's
# header comment) — this file exists so Railway can install everything in one
# pip command without touching what Vercel builds.
-r requirements.txt
ciocore==9.1.1
