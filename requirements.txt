mcp==1.27.1
httpx>=0.27.0
uvicorn>=0.29.0
cryptography>=42.0.0
# Pinned to match ciocore's own hard pin (pyjwt==2.9.0). Left as >=2.8.0 it
# resolves to 2.13.0 and the install conflicts.
PyJWT==2.9.0
# Conductor SDK — needed to upload a job's files. See DEPLOY-RAILWAY.md.
ciocore==9.1.1
